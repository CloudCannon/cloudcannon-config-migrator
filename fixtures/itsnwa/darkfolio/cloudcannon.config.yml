collections_config:
  projects:
    name: Projects
    path: projects
    schemas:
      project:
        path: .cloudcannon/schemas/project.md
        name: Project
        _enabled_editors:
          - content
        _inputs:
          $.categories:
            type: array
collection_groups:
  - heading: Content
    collections:
      - projects
paths:
  static: static/
  uploads: uploads
_inputs:
  template:
    hidden: true
