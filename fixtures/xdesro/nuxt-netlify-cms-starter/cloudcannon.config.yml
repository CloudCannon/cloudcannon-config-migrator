collections_config:
  blog:
    name: Blog
    path: assets/content/blog
    schemas:
      default:
        path: .cloudcannon/schemas/blog.json
        name: Blog
        _enabled_editors:
          - data
        _inputs:
          $.date:
            label: Publish Date
    text_key: title
paths:
  static: static
  uploads: static/img
